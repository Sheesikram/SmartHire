import React from 'react';

const del_subscription = () => {
    return (
        <div style={{ display: "flex", justifyContent: "center", alignItems: "center", height: "100vh" }}>
            <h1 style={{ fontSize: "48px", fontWeight: "bold" }}>
            del_subscription 
            </h1>
        </div>
    );
};

export default del_subscription;